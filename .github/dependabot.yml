# Actions only. The Spark image is pinned to 4.2.0 on purpose (re-vendor at 4.5.0), Mix deps
# move when a probe says they can, and `latu` moves when this package needs something new of it.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
